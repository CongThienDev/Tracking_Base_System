# 04 - Frontend Console Operations

You are a Senior Frontend Engineer.

Goal:
Make the console usable for daily operator workflows.

Tasks:

1. Improve Events UX (filters, sort, pagination, detail)
2. Keep API calls typed and resilient
3. Show clear loading/error/empty states
4. Support replay actions for failed deliveries
5. Avoid breaking existing routes

Constraints:

- Keep architecture layers (`app/pages/widgets/features/entities/shared`)
- No UI-only fake behavior for critical ops actions

Done criteria:

- Typecheck passes
- Build passes
- Event investigation flow works end-to-end
